# DEBUG controls the server, and will log each request if turned on
DEBUG = False
# LOG_LEVEL controls the application logging
LOG_LEVEL = "INFO"
HOST = "0.0.0.0"
PORT = "5000"
# The directory that the credentials will be cached in. If you are running this locally,
# you should probably adjust this to being in your home directory to keep them safe
SPOTIPY_CACHE = "/tmp/spotipy-cache"
